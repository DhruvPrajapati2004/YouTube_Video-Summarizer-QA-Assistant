streamlit==1.35.0
python-dotenv==1.0.1

# --- Core transcript and video processing ---
# For fetching transcripts from YouTube videos
youtube-transcript-api==0.6.2
# For fetching video metadata like titles and thumbnails
pytube==15.0.0
# A powerful tool for downloading video streams, used as a fallback for transcripts
yt-dlp==2024.07.25

# --- LangChain and AI modules ---
# The main LangChain library for building the RAG chain
langchain==0.2.1
# Core data structures for LangChain
langchain-core==0.2.3
# Community-maintained integrations
langchain-community==0.2.1
# Integration for the Groq LLM API
langchain-groq==0.1.5
# Utility for splitting text into chunks
langchain-text-splitters==0.2.0
# The official Groq API client
groq==0.8.0

# --- Vector Store and Embeddings ---
# CPU version of the FAISS library for efficient vector search
faiss-cpu==1.8.0
# For generating text embeddings to be stored in FAISS
sentence-transformers==2.7.0

# --- Scientific computing (pinned for compatibility) ---
# A fundamental package for scientific computing, pinned to a version with pre-built wheels
numpy==1.26.4

# --- Networking ---
# For making HTTP requests, used by various libraries
requests==2.32.3
# HTTP client library, a dependency of requests
urllib3<2.0
